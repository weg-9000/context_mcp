# Agent Logger - 범용 AI 에이전트 로깅 시스템 의존성

# 기본 의존성
dataclasses>=0.6; python_version < "3.7"
typing-extensions>=4.0.0

# 데이터베이스 어댑터 (선택사항)
azure-cosmos>=4.0.0  # CosmosDB
psycopg2-binary>=2.9.0  # PostgreSQL
pymongo>=4.0.0  # MongoDB
redis>=4.0.0  # Redis
# sqlite3은 Python 내장 모듈

# 유틸리티
python-dateutil>=2.8.0

# 로깅 및 JSON 처리
# logging은 Python 내장 모듈
# json은 Python 내장 모듈
# asyncio는 Python 내장 모듈

# 선택적 의존성 (고급 기능용)
# azure-identity>=1.0.0  # Azure 인증 (선택사항)
# boto3>=1.26.0  # AWS 서비스 (선택사항) 